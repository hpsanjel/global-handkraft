// Shared Realtime channel topic names, safe to import from both server
// (route handlers broadcasting) and client (components subscribing) code.

export const ADMIN_CUSTOM_REQUESTS_TOPIC = "admin:custom-requests";
